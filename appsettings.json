{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",

  "Authentication": {
    "//": "No tokens configured here on purpose - this file is the shared base config for every environment, and a real token should never sit in source control. Set real values via environment variables (Authentication__Tokens__0__Name / Authentication__Tokens__0__Token, etc.), `dotnet user-secrets`, or your hosting platform's secret store. appsettings.Development.json supplies a throwaway token for local runs only.",
    "Tokens": []
  }
}
